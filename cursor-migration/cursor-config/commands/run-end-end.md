# /run-end-end
Run the end-to-end pipeline for 1 reel.

## Your Goal
- Use the exact entrypoint below for a single reel
- Command: `python -m src.scripts.run_news_pipeline --max-items 1 --generate-av`
- Report success/failure and key output paths
- If config is missing, ask once for required inputs
